import { HoverMouseDirective } from './hover-mouse.directive';

describe('HoverMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverMouseDirective();
    expect(directive).toBeTruthy();
  });
});
